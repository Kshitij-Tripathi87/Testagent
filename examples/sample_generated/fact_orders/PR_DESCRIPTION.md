## Auto-generated contract: `fact_orders`

Generated by `dhqa generate --dataset urn:li:dataset:(urn:li:dataPlatform:snowflake,orders.fact_orders,PROD)` from the DataHub-declared contract (schema + lineage + ownership).

### What this PR does

- Adds a dbt model `models/fact_orders.sql` mirroring the DataHub schema for `urn:li:dataset:(urn:li:dataPlatform:snowflake,orders.fact_orders,PROD)`.
- Adds a dbt `schema.yml` with column-level data tests (not_null, unique, relationships).
- Adds a pytest suite `tests/test_fact_orders.py` that runs the same checks against the source data for CI.

### Lineage impact

This model depends on: stg_orders.

If any of those upstream datasets changes shape or contract, this model's dbt tests will fail because the lineage is wired into `relationships(to=ref('<upstream>'), field='<col>')`.

### Test coverage added

- `not_null` on `customer_id`
- `not_null` on `id`
- `not_null` on `order_ts`
- `unique` on `id`
- `relationships` on `id` -> `stg_orders`
- `freshness` on `order_ts` (SLA: 24h)

### Risk preview

- **Schema drift** in upstream `raw_orders`: caught by the dbt contract.
- **Data quality regressions** in upstream columns: caught by `not_null` and `relationships` tests.
- **SLA breach** (pipeline runs late): caught by the freshness test.

---
Files: `models/fact_orders.sql`, `tests/test_fact_orders.py`, `schema.yml` (dhqa/generated -> main)

(Generated by `dhqa` from DataHub metadata.)
